import styled from "styled-components";


export const StyleCard = styled.div`
width: ${({width})=>width};  
  box-shadow: 0 2px 4px 0 rgba(0, 0, 0, 0.1), 0 6px 20px 0 rgba(0, 0, 0, 0.16);
  height: 250px;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 15px;

`